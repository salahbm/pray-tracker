{
  "name": "prayer-tracker",
  "private": true,
  "packageManager": "bun@1.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "turbo": "^latest"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  }
}
