doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')

    title Reset Your Password

    style.
      /* Root colors */
      :root {
        --brand: #9EF010;
        --text: #1f2937;
        --muted: #4b5563;
        --bg: #f6f7f8;
        --white: #fff;
      }

      body {
        margin: 0;
        padding: 0;
        background: var(--bg);
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        color: var(--text);
      }

      .email-wrapper {
        padding: 32px 16px;
      }

      .card {
        max-width: 560px;
        margin: 0 auto;
        background: var(--white);
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        overflow: hidden;
      }

      .header {
        text-align: center;
        background: linear-gradient(135deg, #a4ff1a, #7fcf0f);
        padding: 36px 24px;
      }

      .logo {
        font-size: 40px;
        margin-bottom: 6px;
      }

      .title {
        color: #ffffff;
        font-size: 22px;
        font-weight: 700;
        margin: 0;
      }

      .content {
        padding: 36px 32px;
      }

      h1 {
        font-size: 22px;
        margin-bottom: 16px;
      }

      p {
        font-size: 15px;
        line-height: 1.6;
        margin: 0 0 18px;
        color: var(--muted);
      }

      .btn {
        display: inline-block;
        padding: 14px 32px;
        background: var(--brand);
        color: #000;
        text-decoration: none;
        font-weight: 600;
        border-radius: 8px;
        margin: 24px 0;
      }

      .btn:hover {
        opacity: 0.9;
      }

      .warning-box {
        background: #fffceb;
        border-left: 4px solid #f59e0b;
        padding: 12px 16px;
        border-radius: 6px;
        margin: 28px 0;
      }

      .code {
        font-family: monospace;
        background: #eef0f3;
        padding: 4px 6px;
        border-radius: 4px;
        font-size: 13px;
      }

      .footer {
        font-size: 13px;
        text-align: center;
        padding: 24px;
        border-top: 1px solid #e5e7eb;
        color: #6b7280;
      }

      /* Dark mode */
      @media (prefers-color-scheme: dark) {
        body {
          background: #0f0f0f;
        }
        .card {
          background: #1c1c1c;
        }
        p, h1, .footer {
          color: #e5e5e5;
        }
        .code {
          background: #3b3b3b;
          color: #fff;
        }
      }

  body
    .email-wrapper
      .card
        .header
          .logo
          p.title Noor Pray Tracker

        .content
          h1 Reset Your Password

          p Hello,

          p We received a request to reset your password for 
            span.code= email

          center
            a.btn(href=resetUrl) Reset Password

          .warning-box
            p
              strong ⚠️ Security Notice
            p This link will expire in 10 minutes. If you didn't request this, please ignore it.

          p If the button above doesn't work, copy and paste this link:
          p
            span.code= resetUrl

        .footer
          p This email was sent by Noor Pray Tracker.
          p Need help? Contact support.
          p © #{new Date().getFullYear()} Noor Pray Tracker. All rights reserved.
