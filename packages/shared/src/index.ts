/**
 * Shared utilities and types consumed by Prayer Tracker applications.
 *
 * This placeholder export ensures the package compiles correctly even if no
 * concrete utilities have been authored yet. Replace this with real exports as
 * shared modules are added.
 */
export const placeholder = true;
